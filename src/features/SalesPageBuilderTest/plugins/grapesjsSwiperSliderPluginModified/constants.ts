export const prefix = "swiper-";

// Component names
export const containerName = `${prefix}container`;
export const wrapperName = `${prefix}wrapper`;
export const slideName = `${prefix}slide`;
export const prevName = `${prefix}prev`;
export const nextName = `${prefix}next`;
export const paginationName = `${prefix}pagination`;
export const scrollbarName = `${prefix}scrollbar`;

// Selectors
export const containerSelector = `[data-${containerName}]`;
export const wrapperSelector = `[data-${wrapperName}]`;
export const slideSelector = `[data-${slideName}]`;
export const prevSelector = `[data-${prevName}]`;
export const nextSelector = `[data-${nextName}]`;
export const paginationSelector = `[data-${paginationName}]`;
export const scrollbarSelector = `[data-${scrollbarName}]`;

// IDs
export const containerId = `data-${containerName}`;
export const wrapperId = `data-${wrapperName}`;
export const slideId = `data-${slideName}`;
export const prevId = `data-${prevName}`;
export const nextId = `data-${nextName}`;
export const paginationId = `data-${paginationName}`;
export const scrollbarId = `data-${scrollbarName}`;

// css classes
export const classSlide = "swiper-slide";

// htmls
export const htmlSlide = `<div class="${classSlide}"></div>`;

// Traits
export const slidesListTraitName = "slides-list";
export const slidesListTraitCtnClassname = "slides-list-container";
